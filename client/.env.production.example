# Use /api when frontend and backend are served from the same domain through a reverse proxy.
VITE_API_BASE_URL=/api

# Use a full URL when frontend and backend are on different domains.
# VITE_API_BASE_URL=https://api-domain.example/api
